To install the Enterprise `arctl` CLI:

```
export VERSION=v2026.5.4
curl -sSL https://storage.googleapis.com/agentregistry-enterprise/install.sh | ARCTL_VERSION=$VERSION sh
export PATH=$HOME/.arctl/bin:$PATH
```
