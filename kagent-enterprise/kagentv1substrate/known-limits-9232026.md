- The UI enforces no authentication. Each person who opens the UI is an administrator. Treat
this installation as unauthenticated, and do not put it on a network that other persons can
reach.

- Install Substrate via the kagent helm chart, not by itself/on its own.

- Claude Code on Vertex AI does not work at this time.