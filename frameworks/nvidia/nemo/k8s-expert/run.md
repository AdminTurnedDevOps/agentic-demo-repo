```
pip install "nvidia-nat[langchain]"
```

pip install "nvidia-nat[webpage_query]"

```
export NVIDIA_API_KEY=<your_api_key>
```

```
nat run --config_file k8s_expert.yaml --input "What can you do?"
```