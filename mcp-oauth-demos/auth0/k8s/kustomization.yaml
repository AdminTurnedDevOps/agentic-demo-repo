apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mcp-demo-auth0

resources:
  - namespace.yaml
  - mcp-server-deployment.yaml
  - mcp-server-service.yaml
  - gateway.yaml
  - mcp-target.yaml
  - gloo-traffic-policy.yaml
  # Uncomment if you've created secrets.yaml (not the template)
  # - secrets.yaml

labels:
  - pairs:
      demo: mcp-oauth
      provider: auth0
