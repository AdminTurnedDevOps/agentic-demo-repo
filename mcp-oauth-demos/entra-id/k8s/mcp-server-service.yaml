apiVersion: v1
kind: Service
metadata:
  name: mcp-server
  namespace: mcp-demo-entra
  labels:
    app: mcp-server
spec:
  selector:
    app: mcp-server
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
